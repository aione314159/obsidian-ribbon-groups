# The obsidian package ships types only, and its peer dependency on
# @codemirror is strict about versions this plugin never touches.
# Allow the peer mismatch rather than pinning CodeMirror for nothing.
legacy-peer-deps=true
